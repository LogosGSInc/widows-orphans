name: backend
description: Dart Frog API server for the Widows & Orphans platform.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_frog: ^1.1.0
  dotenv: ^4.2.0
  dart_jsonwebtoken: ^2.12.0

dev_dependencies:
  build_runner: ^2.4.6
  dart_frog_gen: ^1.1.0
  lints: ^3.0.0
  mocktail: ^1.0.1
  test: ^1.24.0
