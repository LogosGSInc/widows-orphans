name: widows_orphans_mobile
description: Widows & Orphans mobile app — a dignified care coordination platform.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <5.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: ../../packages/domain
  supabase_flutter: ^2.3.0
  flutter_riverpod: ^2.4.9
  riverpod_annotation: ^2.3.3
  go_router: ^13.0.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  build_runner: ^2.4.6
  riverpod_generator: ^2.3.9
  lints: ^3.0.0
  mocktail: ^1.0.1

flutter:
  uses-material-design: true
