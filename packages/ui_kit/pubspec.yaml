name: ui_kit
description: Shared Flutter widget library for the Widows & Orphans platform.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <5.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^3.0.0
