name: api_client
description: Generated Dart API client for the Widows & Orphans backend.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <5.0.0"

dependencies:
  http: ^1.1.0
  domain:
    path: ../domain

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
